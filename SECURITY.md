# Security reports

Use the repository's **Security → Report a vulnerability** option for a private security report. Include reproduction steps with synthetic data and the affected commit. Never include live tokens, artist data, or store exports.

Deployments are operated by their own hosts. Keep dependencies current, store credentials in Cloudflare Worker secrets, use HTTPS, and back up D1. Rotate any accidentally exposed credentials promptly.
